<%# llms.txt — discovery file pointing AI crawlers at the full markdown %>
<%# bundle plus per-page markdown URLs. Convention: https://llmstxt.org %>
<% host = "#{request.protocol}#{request.host_with_port}" -%>
# OpenSOP

> Define a process, get an API. Agents and humans interact through the same `/sop/*` endpoints.

OpenSOP is an open standard and runtime for defining business processes (SOPs) as YAML and exposing them as REST APIs. This file points crawlers and AI agents at the machine-readable documentation.

The full markdown bundle of every guide and endpoint lives at <%= host %>/api-docs.md.

## Project

- **Source:** https://github.com/Chosen9115/opensop
- **Issues:** https://github.com/Chosen9115/opensop/issues
- **License:** [MIT](https://github.com/Chosen9115/opensop/blob/main/LICENSE)
- **API spec version:** <%= defined?(Opensop::DefinitionParser::SPEC_VERSION) ? Opensop::DefinitionParser::SPEC_VERSION : "0.1" %>
<% if defined?(Opensop::VERSION) -%>
- **Engine version:** v<%= Opensop::VERSION %>
<% end -%>

## Docs

- [Full bundle](<%= host %>/api-docs.md): every guide and endpoint in one file.

## Guides

<% Ui::ApiDocs::Catalog::GUIDES.each do |guide| -%>
- [<%= t("opensop.api_docs.guides.#{guide[:slug].underscore}.title", default: guide[:label]) %>](<%= host %>/api-docs/guides/<%= guide[:slug] %>.md)
<% end -%>

## Endpoints

<% Ui::ApiDocs::Catalog::ENDPOINT_SECTIONS.each do |section| -%>

### <%= section[:label] %>

<% section[:endpoints].each do |ep| -%>
- [`<%= ep[:method] %> <%= ep[:path] %>`](<%= host %>/api-docs/endpoints/<%= ep[:slug] %>.md): <%= t("opensop.api_docs.endpoints.#{ep[:slug].underscore}.title", default: ep[:slug].tr('-', ' ').capitalize) %>
<% end -%>
<% end -%>
